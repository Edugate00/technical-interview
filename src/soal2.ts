export function twoSum(nums: number[], target: number): number[] {
  // TODO: implementasikan logika untuk mencari dua indeks
  // yang jika dijumlahkan menghasilkan nilai 'target'

  // petunjuk:
  // - kamu bisa gunakan Map untuk menyimpan angka yang sudah dilihat
  // - jika ditemukan pasangan yang sesuai, return [indexPasangan, indexSekarang]

  return []; // ubah sesuai hasil
}
